gem:
  # Add your `gem` command parameters here:
  #
  exclude:
    - rdoc # RDoc::Servlet required webrick, which we don't have
  # doc: true
  # workers: 5
dsl:
  # Add your `dsl` command parameters here:
  #
  # exclude:
  # - SomeGeneratorName
  # workers: 5
